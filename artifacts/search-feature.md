# Фича: Поиск сообщений в чате 🔍

## Обзор

Дополнительная функция Telegram-бота для аналитики чатов - **команда `/search`** позволяет пользователям искать сообщения в групповом чате по ключевым словам или регулярным выражениям.

## Функциональность

### Основные возможности
- **Простой текстовый поиск** по ключевым словам
- **Поиск с регулярными выражениями** для сложных запросов
- **Выделение найденного текста** жирным шрифтом в результатах
- **Пагинация результатов** (по 5 сообщений на страницу)
- **Кэширование результатов** в Redis для производительности

### Синтаксис команд

#### Простой поиск
```
/search ключевое_слово
/search несколько слов
```

#### Поиск с регулярными выражениями
```
/search /регулярное_выражение/
```

### Примеры использования

#### Пример 1: Простой поиск
```
/search привет
```
Найдет все сообщения, содержащие слово "привет" (регистронезависимо).

#### Пример 2: Поиск нескольких слов
```
/search машинное обучение
```
Найдет сообщения, содержащие любое из слов "машинное" или "обучение".

#### Пример 3: Регулярное выражение
```
/search /прив.*мир/
```
Найдет сообщения, соответствующие regex: "прив" + любые символы + "мир"
- "привет мир"
- "приветики мир"
- "приветствую мир"

#### Пример 4: Поиск чисел
```
/search /\d{4}/
```
Найдет сообщения, содержащие 4-значные числа (годы, PIN-коды и т.д.)

## Архитектура реализации

### Компоненты

#### 1. Обработчик команд (`handlers/search.ts`)
- `handleSearchCommand()` - обработка команды `/search`
- `handleSearchCallback()` - обработка inline-кнопок пагинации
- `getSearchResults()` - получение результатов с кэшированием
- `formatSearchMessage()` - форматирование сообщений с результатами

#### 2. Модель данных (`models/Message.ts`)
- `searchMessages()` - основной метод поиска в БД
- `countSearchResults()` - подсчет общего количества результатов
- `highlightText()` - выделение найденного текста
- `escapeRegExp()` - экранирование специальных символов

#### 3. Сервис кэширования (`services/redis.ts`)
- `createSearchCacheKey()` - генерация ключей кэша для поиска

### База данных

#### SQL запросы для поиска

**Простой поиск:**
```sql
SELECT m.id, m.user_id, u.username, u.first_name, u.last_name,
       m.text, m.created_at
FROM messages m
JOIN users u ON m.user_id = u.id
WHERE m.chat_id = $1 AND m.text ILIKE $2
ORDER BY m.created_at DESC
LIMIT $3 OFFSET $4
```

**Regex поиск:**
```sql
SELECT m.id, m.user_id, u.username, u.first_name, u.last_name,
       m.text, m.created_at
FROM messages m
JOIN users u ON m.user_id = u.id
WHERE m.chat_id = $1 AND m.text ~ $2
ORDER BY m.created_at DESC
LIMIT $3 OFFSET $4
```

### Кэширование

- **Ключ кэша**: `search:{chatId}:{normalizedQuery}:{page}`
- **TTL**: 10 минут
- **Нормализация запроса**: убираются пробелы и специальные символы

### Пользовательский интерфейс

#### Inline-клавиатура
```
🔍 Результаты поиска (из кэша)
📝 Запрос: "ключевые слова"
📊 Найдено: 15 сообщений
📄 Показаны: 1-5

💬 *Результаты:*
1. **@username** (22.01.2026 10:30)
   Привет, **как** дела сегодня?

[⬅️ Назад] [2/3] [Вперёд ➡️]
[🔍 Новый поиск]
```

#### Сообщение помощи
```
/search
```

Показывает инструкцию по использованию с примерами.

## Производительность

### Оптимизации
- **Кэширование результатов** на 10 минут
- **Пагинация** по 5 результатов на страницу
- **ILIKE для регистронезависимого поиска**
- **PostgreSQL regex** для сложных выражений

### Ограничения
- Максимум 50 символов в поисковом запросе
- Результаты сортируются по дате (новые сверху)
- Только для групповых чатов

## Тестирование

### Unit-тесты (`Message.test.ts`)
- `highlightText()` - выделение текста
- `escapeRegExp()` - экранирование regex
- Множественные вхождения слов
- Обработка некорректных regex

### Ручное тестирование
- Проверка различных типов запросов
- Тестирование пагинации
- Проверка кэширования
- Валидация работы в групповых чатах

## Безопасность

### Валидация ввода
- Проверка корректности регулярных выражений
- Экранирование специальных символов
- Ограничение длины поискового запроса

### Защита от злоупотреблений
- Кэширование предотвращает повторные тяжелые запросы
- Ограничение на количество результатов на страницу

## Расширение функциональности

### Возможные улучшения
1. **Фильтры по дате** - поиск в определенный период
2. **Фильтры по пользователям** - поиск сообщений конкретного пользователя
3. **Экспорт результатов** - сохранение в файл
4. **Статистика поиска** - наиболее популярные запросы

### API для веб-интерфейса
```typescript
// Возможный endpoint для веб-приложения
GET /api/search?query=ключ&chatId=123&page=1
```

## Технические детали

### Зависимости
- `pg` - PostgreSQL клиент
- `redis` - кэширование
- `telegraf` - Telegram Bot API

### Миграции БД
Текущая структура таблицы `messages` подходит без изменений:
```sql
CREATE TABLE messages (
  id SERIAL PRIMARY KEY,
  user_id INTEGER REFERENCES users(id),
  chat_id BIGINT NOT NULL,
  text TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Конфигурация
```env
# Redis для кэширования результатов поиска
REDIS_HOST=redis
REDIS_PORT=6379
```

## Заключение

Функция поиска сообщений значительно расширяет возможности бота по аналитике чатов, позволяя пользователям быстро находить нужную информацию в истории переписки. Реализация включает современные подходы к производительности и пользовательскому опыту.