# Telegram Chat Analytics Bot 🤖

Бот для аналитики Telegram-чатов с интеграцией ИИ. Позволяет собирать статистику сообщений, анализировать пользователей и искать сообщения в чате.

## ✨ Возможности

### 📊 Статистика чата
- Топ активных участников
- Статистика по периодам (сегодня/неделя/месяц/всё время)
- Детальная статистика конкретного пользователя

### 🔍 Поиск сообщений
- **Простой поиск**: `/search ключевое_слово` - поиск по словам
- **Регулярные выражения**: `/search /регулярное выражение/` - продвинутый поиск
- **Выделение результатов**: найденный текст выделяется **жирным шрифтом**
- **Пагинация**: удобная навигация по результатам
- **Кэширование**: результаты поиска кэшируются на 10 минут

### 🤖 ИИ-анализ пользователей
- Анализ стиля общения
- Определение тем разговоров
- Оценка активности и поведения
- Персонализированные рекомендации

## 🚀 Быстрый запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd telegram-chat-analytics
   ```

2. **Настройте переменные окружения:**
   ```bash
   # Создайте файл .env в корне проекта со следующими переменными:
   ```

   ```env
   # Database
   POSTGRES_HOST=postgres
   POSTGRES_PORT=5432
   POSTGRES_DB=chat_analytics
   POSTGRES_USER=postgres
   POSTGRES_PASSWORD=password

   # Redis
   REDIS_HOST=redis
   REDIS_PORT=6379

   # Telegram Bot
   TELEGRAM_BOT_TOKEN=your_bot_token_here

   # Google Gemini AI
   GEMINI_API_KEY=your_gemini_api_key_here

   # Web App
   NEXT_PUBLIC_API_URL=http://localhost:3000
   ```

3. **Запустите сервисы:**
   ```bash
   docker-compose up --build
   ```

4. **Бот готов к работе!** Добавьте его в свой Telegram-чат.

## 📋 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветственное сообщение с инструкциями |
| `/stats` | Показать статистику чата |
| `/search <запрос>` | Поиск сообщений в чате |
| `/analyze @username` | ИИ-анализ пользователя |
| `/analyze` (reply) | Анализ автора сообщения |

## 🔍 Использование поиска

### Простой поиск
```
/search привет
```
Найдет все сообщения, содержащие слово "привет".

### Поиск нескольких слов
```
/search машинное обучение
```
Найдет сообщения с любым из слов "машинное" или "обучение".

### Регулярные выражения
```
/search /прив.*мир/
```
Найдет сообщения, соответствующие регулярному выражению (пример: "привет мир", "привки мир").

### Примеры регулярных выражений
- `/search /\\d{4}/` - найти 4-значные числа
- `/search /привет\|здравствуй/i` - найти "привет" или "здравствуй" (регистронезависимо)
- `/search /кот.*собак/` - найти "кот" за которым следует "собак"

## 🏗️ Архитектура

```
telegram-chat-analytics/
├── docker-compose.yml           # Оркестрация сервисов
├── bot/                         # Node.js Telegram бот
│   ├── src/
│   │   ├── index.ts            # Главный файл бота
│   │   ├── config.ts           # Конфигурация
│   │   ├── models/             # Модели данных
│   │   │   ├── User.ts         # Модель пользователя
│   │   │   └── Message.ts      # Модель сообщения
│   │   ├── services/           # Бизнес-логика
│   │   │   ├── database.ts     # Подключение к БД
│   │   │   ├── redis.ts        # Кэширование
│   │   │   └── gemini.ts       # LLM интеграция
│   │   ├── handlers/           # Обработчики команд
│   │   │   ├── stats.ts        # Статистика
│   │   │   └── search.ts       # Поиск сообщений
│   │   └── utils/              # Утилиты
│   ├── package.json
│   └── Dockerfile
├── web/                         # Next.js веб-интерфейс
│   ├── pages/
│   │   ├── index.tsx           # Главная страница
│   │   └── api/
│   │       └── analyze.ts       # API для анализа
│   ├── components/             # React компоненты
│   ├── lib/                    # Общие функции
│   └── package.json
├── .env.example                 # Пример переменных окружения
└── README.md                    # Документация
```

## 🛠️ Технологии

- **Backend**: Node.js, TypeScript
- **База данных**: PostgreSQL
- **Кэширование**: Redis
- **Telegram API**: Telegraf
- **ИИ**: Google Gemini AI
- **Веб-интерфейс**: Next.js, React
- **Контейнеризация**: Docker, Docker Compose

## ⚙️ Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Database
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=chat_analytics
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Google Gemini AI
GEMINI_API_KEY=your_gemini_api_key_here

# Web App
NEXT_PUBLIC_API_URL=http://localhost:3000
```

## 🤖 Использование AI-инструментов

Проект активно использует AI-инструменты для ускорения разработки и повышения качества кода.

### Какие AI-инструменты использовались:

- **Cursor IDE** - основная среда разработки с AI-помощником
- **Claude Code** - для генерации кода и архитектурных решений
- **GitHub Copilot** - для автодополнения и генерации boilerplate кода

### Для каких задач AI помог больше всего:

1. **Генерация моделей данных** - AI помог создать полные CRUD модели для User и Message с типизацией и валидацией
2. **Архитектура сервисов** - AI предложил оптимальную структуру с разделением на сервисы (database, redis, gemini)
3. **SQL запросы** - AI сгенерировал сложные запросы для поиска, статистики и индексов БД
4. **Обработчики команд** - AI создал полную логику для команд /stats, /analyze, /search с inline-клавиатурами
5. **Docker конфигурация** - AI помог настроить multi-stage Dockerfiles и docker-compose с healthchecks
6. **Тесты** - AI сгенерировал comprehensive unit-тесты с моками и edge cases
7. **Документация** - AI помог составить подробный README и документацию функций

### Где пришлось доработать сгенерированный код:

1. **Типизация** - AI-код потребовал доработки для полного соответствия строгой TypeScript типизации
2. **Обработка ошибок** - Добавил более детальную обработку ошибок и graceful degradation
3. **Кэширование** - Улучшил логику кэширования для предотвращения race conditions
4. **Валидация ввода** - Добавил дополнительную валидацию для regex выражений и пользовательского ввода
5. **Производительность** - Оптимизировал запросы к БД и добавил индексы для поиска

### Примерная оценка: сколько времени сэкономил AI

**Общее время разработки: ~19-27 часов**

- **Сгенерировано AI**: ~15-20 часов кода и архитектуры
- **Доработка и интеграция**: ~4-7 часов
- **Экономия времени**: ~60-70% от времени ручной разработки

**Конкретные примеры экономии:**
- Модели данных: 2 часа вместо 6 часов
- Сервисы (DB, Redis, Gemini): 3 часа вместо 8 часов
- Обработчики команд: 4 часа вместо 10 часов
- Docker конфигурация: 1 час вместо 3 часов
- Тесты: 2 часа вместо 5 часов
- Документация: 1 час вместо 2 часов

AI значительно ускорил разработку, особенно в рутинных задачах, позволив сосредоточиться на бизнес-логике и интеграции компонентов.

## 📈 Производительность

- **Кэширование**: Все запросы кэшируются в Redis (статистика - 20 мин, поиск - 10 мин)
- **Оптимизация запросов**: Используются индексы в PostgreSQL для быстрого поиска
- **Пагинация**: Результаты поиска разбиваются на страницы по 5 сообщений
- **Регулярные выражения**: PostgreSQL regex для эффективного поиска

## 🔧 Разработка

```bash
# Установка зависимостей
cd bot && npm install

# Сборка проекта
npm run build

# Запуск в режиме разработки
npm run dev

# Запуск тестов
npm test
```

## 📝 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🤝 Вклад в проект

1. Форкните проект
2. Создайте ветку для вашей фичи (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте логи контейнеров: `docker-compose logs`
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте подключение к Telegram Bot API

---

*Разработано с ❤️ и 🤖*